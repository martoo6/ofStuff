#ifndef CUBIC_H
#define CUBIC_H

#include <Interpolation.h>

/*
class Cubic : public Interpolation
{
    public:
        Cubic();
        virtual ~Cubic();
        float getValue(float x, float* values);
        float getValue(float x, float y, float** values);
        float getValue(float x, float y, float z, float*** values);
    protected:
    private:
        float cubicInterpolation(float y1, float y2, float y3, float y4, float x);
};
*/
#endif // CUBIC_H
